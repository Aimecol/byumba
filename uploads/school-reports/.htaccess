# Deny direct access to uploaded files
Options -Indexes
<Files *.php>
    Deny from all
</Files>

# Allow only specific file types
<FilesMatch "\.(pdf|doc|docx|xls|xlsx|ppt|pptx|txt|jpg|jpeg|png)$">
    Order Allow,Deny
    Allow from all
</FilesMatch>
